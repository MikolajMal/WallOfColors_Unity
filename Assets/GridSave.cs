using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GridSave : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("TO DO: Zapisywanie aktualnego stanu gry dla odpowiedniego poziomu trudnoœci");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
